---
"@jmfederico/pi-web": patch
---

Open the SysIDE workspace panel on a model overview by default: it now shows the loaded model's packages and per-type element counts on first load, with the previous check-error view as the fallback when no model is loaded or the survey fails. The toolbar gains an Overview button, and Check/Elements now select their views directly.
